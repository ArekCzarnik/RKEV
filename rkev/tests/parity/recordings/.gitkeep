# The recordings a parity session writes. They belong in git once they exist -
# see tests/parity/README.md - so nothing is ignored here; this file only keeps
# the empty directory from disappearing before the first session.
